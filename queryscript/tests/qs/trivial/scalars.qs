let foo = 1;
SELECT foo;

let bar = [1,2,3];
select bar;

let baz = ['a', 'b', 'c'];
select baz;

select [['a', 'b'], ['c', 'd']];
select [['a', 'b'], ['c', 'd'], ];


[1,2,3];
['a', 'b', 'c'];
[['a', 'b'], ['c', 'd']];
[['a', 'b'], ['c', 'd'], ];
